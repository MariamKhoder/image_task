  on:
    push:
      branches:
        - master




  jobs:
    build:
      runs-on: ubuntu-latest


      steps:
        - uses: actions/checkout@v3

        - name: "printing folder content"
          run: ls

        - name: "login to Docker hub" 
          run: docker login --username ${{ secrets.MARIAM_REDA_MOHAMED_2023040387 }} --password ${{ secrets.PASS }}

        - name: "building image"
          run: docker build -t ${{ secrets.MARIAM_REDA_MOHAMED_2023040387 }}/mariam_reda_mohamed .

        - name: "pushing to docker hub"  
          run: docker push ${{ secrets.MARIAM_REDA_MOHAMED_2023040387 }}/mariam_reda_mohamed




